# Colab-only deps for MuRIL fine-tuning (GPU). Do NOT use requirements.txt on Colab.
# Pinned versions avoid peft/accelerate import conflicts on Colab Python 3.12.

transformers==4.44.2
accelerate==0.34.2
datasets==2.19.0
pandas>=2.0.0
numpy>=1.24.0,<2.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0
tqdm>=4.66.0
